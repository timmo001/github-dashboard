query User($user: String!) {
  rateLimit {
    cost
    remaining
  }
  user(login: $user) {
    avatarUrl
    login
    name
    url
    followers {
      totalCount
    }
    following {
      totalCount
    }
    repositories(first: 100, orderBy: { field: PUSHED_AT, direction: DESC }) {
      totalCount
      nodes {
        name
        owner {
          login
        }
      }
    }
  }
}
